# Configuration Files (configs/)

## Purpose
Declarative configuration files specifying dataset locations, model hyperparameters,
hardware thresholds, and experiment parameters.

## Key Files
- `data.yaml`: YOLO dataset specification mapping class indices to banknote denominations.
