# Dataset Directory (data/)

## Purpose
Storage location for raw and processed Azerbaijani banknote images, YOLO format labels,
and stratified dataset manifests.

## Structure
- `raw/`: Original captured images and initial annotations.
- `processed/`: Zero-leakage stratified splits (train, val, test) with normalized labels.
